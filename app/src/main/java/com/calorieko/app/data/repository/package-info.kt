package com.calorieko.app.data.repository

/**
 * Repository layer for CalorieKo.
 *
 * This package will contain repositories that abstract data sources
 * (Firebase, Room, SharedPreferences) behind a clean API.
 *
 * TODO: Create repositories as needed:
 *   - UserRepository        → Firebase Auth + Firestore user profiles
 *   - MealRepository        → Logged meals (local + cloud)
 *   - WorkoutRepository     → Workout sessions
 *   - RecipeRepository      → Recipe data
 */
