package com.calorieko.app.viewmodel

/**
 * ViewModel layer for CalorieKo.
 *
 * This package will contain ViewModels that manage UI state and
 * coordinate between the data layer and the UI.
 *
 * TODO: Create ViewModels as needed:
 *   - UserViewModel         → Profile data, auth state
 *   - DashboardViewModel    → Daily summary, activity feed
 *   - MealViewModel         → Meal logging state
 *   - WorkoutViewModel      → Workout tracking state
 *   - ProgressViewModel     → Charts and analytics data
 */
